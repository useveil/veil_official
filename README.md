# Veil Official Site

This directory is the standalone official website for Veil. It is intentionally
separate from the Tauri desktop app so it can be built and deployed independently.

## Commands

```bash
npm install
npm run dev
npm run build
npm run preview
```

## Deploy

Use this directory as the Vercel project root:

```bash
vercel --yes
```
